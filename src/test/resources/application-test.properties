# Datasource connection details are supplied by @ServiceConnection on the
# MySQLContainer in AssetTaggingSystemApplicationTests, not by properties here.
# The base application.properties' MySQL dialect, ddl-auto=validate, and Flyway
# settings all still apply as-is -- the test now runs against real MySQL, so
# nothing needs overriding for a different database engine anymore.
