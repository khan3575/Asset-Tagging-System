# Copy this file to .env on the server (never commit .env itself) and fill
# in real values. compose.yaml reads these to configure both the db and app
# services -- see compose.yaml for exactly how each one is used.

DB_NAME=asset_tagging_system
DB_USER=ats_app
DB_PASS=changeme
DB_ROOT_PASS=changeme
